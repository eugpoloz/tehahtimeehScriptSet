{
  "name": "tehahtimeeh-monorepo",
  "private": true,
  "packageManager": "yarn@4.17.1",
  "workspaces": [
    "lib/*",
    "scripts/*"
  ],
  "scripts": {
    "build": "make build",
    "clean": "make clean",
    "typecheck": "tsc --noEmit",
    "format": "prettier --write \"lib/**/*.{js,json}\" \"scripts/**/*.{js,json}\" \"tooling/**/*.{js,mjs}\" \"*.{js,json,md}\" --ignore-path .gitignore"
  },
  "devDependencies": {
    "prettier": "^3.8.4",
    "typescript": "^6.0.3"
  },
  "prettier": {
    "tabWidth": 2,
    "useTabs": false,
    "trailingComma": "none"
  }
}
